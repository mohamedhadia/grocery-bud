# cra with tailwindcss v2 starter files using craco

download > npm install > npm start for a dev server.

npm run build to build the app but make sure to enable "purge" mode from the "tailwind.config.js" file
